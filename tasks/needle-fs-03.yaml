# Frozen fixture.
id: needle-fs-03
category: needle
prompt: >
  According to the sandbox project log, was Milestone 1 shipped early or
  late — and by how much?
grading:
  type: contains
  expected: ["two days", "early"]
expected_tools: ["fs:read_text_file"]
timeout_turns: 10
