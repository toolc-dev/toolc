# Benchmark sandbox

Fixture directory served by `@modelcontextprotocol/server-filesystem` in the
benchmark federation. Contents are frozen — benchmark `needle` tasks assert
against specific facts in these files. Do not edit without re-verifying tasks.
